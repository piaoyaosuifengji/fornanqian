2:
#include<stdio.h>
void main()
{
	int i,j,a;
	printf("input a:\n" );
	scanf("%d",&a);


printf("output: ");

	for(i=2;i<=a;)
	{
		if(a%i==0&&a!=i)
	{	
printf("%d*",i);	
a=a/i;	
i=2;
} 
	else
     	i++;
if(a==i)	
printf("%d\n",i); 
}
}
	
3:

#include <stdio.h>
int main(){
        int i;
        int j;
        int a[1000]={0};
        for(i=0;i<1000;i++){
                a[i]=i;
        }
        for(j=2;j<35;j++){
                for(i=j*j;i<1000;i+=j){
                        a[i]=0;
                }
        }
                for(i=0;i<1000;i++){
                        if (a[i]!=0 &&a[i]!=1){
                                printf("%d\n",a[i]);
                        }
                }
}




4:

#include <stdio.h>
int main() 
{ 
    int m, n,num1, num2, r; 
    printf("Enter two number:\n"); 
    scanf("%d %d", &m, &n); 
     if (m > 0 && n >0) 
       { 
          num1 = m; 
          num2 = n; 
           r = num1 % num2; 
          while (r != 0) 
      { 
       num1= num2; 
       num2 = r; 
         r = num1 % num2; 
       } 
      printf("最大公约数: %d\n", num2); 
       printf("最小公倍数 : %d\n", m * n / num2); 
    } 
      else printf("Error!\n"); 
} 

5:
#include<stdio.h>
void main ()
{
    double a,b,c,d;
     printf("ENTER THREE NUMBERS :");
    scanf("%lf%lf%lf",&a,&b,&c);
     d=(a+b+c);
     d=d/3;
      d=d*10;
     d=d+0.5;
     d=(int)d;
     d=d/10;
      printf("%lf",d);
}

